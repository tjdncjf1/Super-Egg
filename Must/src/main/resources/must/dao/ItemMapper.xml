<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="must.dao.ItemDao">
  <resultMap type="item" id="itemResultMap">
    <id property="pId"             column="PROD_ID"/>
    <result property="title"       column="TITLE"/>
    <result property="image"       column="IMAGE_URL"/>
    <result property="min_price"   column="MIN_PRICE"/>
    <result property="wish_price"  column="W_PRICE"/>
    <result property="link"        column="LINK"/>
  </resultMap>

	<select id="insert" parameterType="item">
		insert into item (PROD_ID, TITLE, IMAGE_URL, MIN_PRICE, W_PRICE, LINK)
		values (#{pId}, #{title}, #{image}, #{min_price}, #{wish_price}, #{link})
	</select>
	
</mapper>